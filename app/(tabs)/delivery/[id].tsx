import { View, Text } from "react-native";
import React from "react";
import { ScrollView, SizableText } from "tamagui";

const ViewOrder = () => {
  return (
    <ScrollView>
      <SizableText>Texrt</SizableText>
    </ScrollView>
  );
};

export default ViewOrder;
